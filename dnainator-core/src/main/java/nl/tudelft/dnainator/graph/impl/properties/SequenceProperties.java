package nl.tudelft.dnainator.graph.impl.properties;

/**
 * This enum holds all propertytypes that can be set on sequence nodes in the graph.
 */
public enum SequenceProperties {
	ID,
	STARTREF,
	ENDREF,
	SEQUENCE,
	BASE_DIST,
	RANK,
	INTERESTINGNESS,
	EDGE_NUM_STRAINS
}
