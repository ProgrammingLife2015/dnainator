package nl.tudelft.dnainator.graph.impl.properties;

/**
 * This enum holds all propertytypes that can be set on annotation nodes in the graph.
 */
public enum AnnotationProperties {
	ID,
	SENSE,
	STARTREF,
	ENDREF
}
