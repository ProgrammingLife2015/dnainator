package nl.tudelft.dnainator.graph.impl;

/**
 * This enum holds all propertytypes that can be set on nodes of the graph.
 */
public enum PropertyTypes {
	NODELABEL,
	ID,
	SOURCE,
	STARTREF,
	ENDREF,
	SEQUENCE,
	RANK,
	SCORE
}
